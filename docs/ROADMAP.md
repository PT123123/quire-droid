# Roadmap

Milestone plan from `docs/SPEC.md` with current status. Living status
notes and per-milestone reports live in `PLAN.md`; this file is the
stable map. Priorities (SPEC §三十五): UI quality > editing experience >
low RAM > low CPU > GPU rendering > maintainability > feature count.

| Milestone | Scope | Status |
|-----------|-------|--------|
| M0 Toolchain | Rust + Slint 1.18, MSVC, renderer matrix, benchmark harness, baselines | ✅ 2026-09-18 |
| M1 Design system | Theme/Colors/Typography/Icons, product-grade shell (mock data) | ✅ 2026-09-18 |
| M2 Shell navigation | Live workspace tree (create/rename/duplicate/delete/favorites/recents), page switching with per-page mock content, Ctrl+P search, context menus, settings + confirm dialogs, empty state, headless visual-regression tool, scenes F/G benchmarks | ✅ 2026-09-19 |
| M3 Local documents | `core/` document model, SQLite storage (workspaces/pages/blocks/metadata), debounced batched persistence, transactions, load-on-restart, settings persistence | ✅ 2026-09-19 (wired same day) |
| M4 Block editor MVP | Command system + undo/redo, single-TextInput editing, Enter/Backspace/merge/split, arrows, clipboard, 9 block kinds, Chinese IME acceptance pass | ✅ 2026-09-19 core · ✅ IME pass 2026-09-20: the user walked `docs/IME_CHECKLIST.md` by hand and it is now fully checked, which was M4's last open item and M8's last non-code gate |
| M5 Notion interactions | Slash menu (command descriptors from Rust), palette wiring on the real command registry, block drag/reorder, block menu | ✅ 2026-09-19 (drag/reorder = ADR-0021; markdown shortcuts + curated slash/Turn-into menus = ADR-0022; collab/AI/color/copy-link stay out per SPEC) |
| M6 Rich text | Inline spans (bold/italic/code/link/strike), mark persistence, format-safe save/load | ✅ 2026-09-19 (link UI + import/export marks in; the run rendering's documented wrap limitation moved on 2026-09-21 — marked lines now break at word boundaries, ADR-0041, and what is left of the wall is in `docs/EDITOR_ARCHITECTURE.md` §"Platform wall") |
| M7 Performance | Virtualization depth, lazy loading, background search (FTS), long-document suite at 1k/5k/10k blocks | ✅ 2026-09-19 · FTS5 index + async search, and the femtovg/skia scene A–G matrix is recorded in `docs/PERFORMANCE.md` (SPEC §六 checklist: all met — **one row re-opened 2026-09-23**, see below); the release profile that produced these numbers is pinned by the A3 audit as ADR-0024 · **2026-09-23 stress pass** (`benchmarks/scripts/stress_ladder.ps1`, raw `benchmarks/results/2026-09-23-stress-ladder-vg-r2.jsonl` + `…-m14-matrix-vg.jsonl`, reading in `docs/PERFORMANCE.md` §"Stress · the ladder past the matrix"): the matrix re-ran clean (30/30 exit 0, typing still 29.7/117 keystrokes/s) and the ladder carried the suite past it — 50 000 rows load and go quiet at 0.57 % in 198 MB, 100 000 rows take **49.8 s of first write-back** before they are idle at 0.95 % in 261 MB, and a 500-page sidebar costs nothing measurable. Three outcomes. (1) The §六 10 000-block memory row is no longer met: the drift-immune within-session slope D − A is +21.8 MB WS / **+22.0 MB private** where M7 measured +7.2 / +9.2, i.e. 1.214× against a ≤1.2× gate, unattributed and filed as a bisect. (2) A claim written earlier in this session was **retracted on measurement**: "a page past 20 000 rows never goes idle" was the old harness sampling inside the first write-back — 20 000 rows settle in 4.8 s, 50 000 in 12.9 s, 100 000 in 49.8 s — which is why the ladder now publishes `settle_wait_ms` instead of assuming idle. (3) Two shapes do not survive, each with its own control: **a page whose every row is a picture** (stride 1) grew to 3 099 MB at 101 s and never reached its own quit timer while the same 10 000 image rows spread over 20 000 rows sat flat at 180 MB (so it is the shape, not the count, and the 32 MiB decode cache is provably not the failing part — the DB stays at 4 KB), and **10 000 coloured code rows under a scroll** hold 92.4–98.5 % of a core with a flat working set and stop servicing a `slint::Timer`, where the identical plain-text page flicks 1.64 M px and exits 0. Neither is fixed; both have a reproduction command in that section. |
| M8 Windows RC | Crash recovery, migrations, packaging/installer, Markdown import/export, settings UI, keyboard shortcut pass | 🔄 in progress 2026-09-20 · delivered: rotating snapshots + startup recovery (ADR-0015), the `session.meta` + rotating log + panic hook and the clean-exit marker (ADR-0018), data-location move to the per-user library (ADR-0020), schema migrations to v5, Markdown import/export, installer + `just dist`, LAN share/pull, settings UI incl. the STORAGE row (data folder / open-folder / back-up-now), Page and Link-to-page blocks, rich clipboard paste, Move page, the shortcut pass, Go Back / Go Forward (SPEC §十六's last palette item that had no implementation: an in-session page history behind two `Navigate` rows and Alt+← / Alt+→, and because every navigation now commits the debounced typing edit first, a page switch can no longer swallow the last 300 ms of typing). The tail A-package is landing: A1 `--portable` ✅, A3 release-profile audit ✅ (pinned as ADR-0024), A5 this refresh, A2 first-paint measurement ✅ plus its phase-split follow-up (the old window-up number understated startup ≈4×; the remaining gap is now owned per phase — a flat ≈410 ms inside `ui.run()` before the first frame, document-independent, and one scaling term `state_new` 5→148 ms; see `docs/PERFORMANCE.md`), a second follow-up that measured the scaling term at four points in one batch and settled it as **linear** — ≈4 ms + 13.1 ms per 1 000 blocks, `repo_open` flat at 51 ms across a 20× document range, and `benchmarks/scripts/audit_results.ps1` now regenerates every table in PERFORMANCE.md from its raw rows), a third that closes the skia question — one batch, both arms from the same source: skia (`--no-default-features --features skia-opengl`) paints 44 ms sooner on an empty shell (440 vs 484 ms) and ties on a 10 000-block page, so it is not worth its ≈2× working set and **femtovg stays the default**; the plain `--features skia` build cannot be measured this way at all — its rendering notifier is silent (`Surface::with_graphics_api` is a no-op on that surface), and `--features skia` without `--no-default-features` does not even disable the femtovg default (rows `benchmarks/results/2026-09-20-first-paint-ab-*`), A6 installer end-to-end ✅ (`install/verify-installer.ps1`; re-run green at `6c115b5` with the current release exe — 7.94 MB setup, installed exe exits 0, embedded icon still pixel-matches `quire.ico`, the `--open` verb imported pages 14→15, uninstall left zero residue), A1 follow-up: `--portable` verified end to end ✅ (`install/verify-portable.ps1`, 24/24, each child run with its own redirected APPDATA so the real per-user library is never in play), A4 visual sweep ✅ (34 scenes light + dark at `42cb158`, 13 pass / 21 fail, report in `.scratch/sweep/report.md`, harness `benchmarks/scripts/sweep.ps1`), re-swept at `6c115b5` after Track A's two fix commits — delta report `.scratch/sweep2/report.md`, method: hash every PNG against the first sweep, so only the 11 scenes that actually changed pixels were re-judged. Fixed: Settings' shortcut overflow, the Move-to popup clamp, the slash description column, the insert menu's disabled rows, find's counter, the dialog's destructive colour. Two of my own findings are retracted on that hash diff (bg-colour swatches, the title-edit line box) — both were 1:1 eyeball misreads of unchanged pixels. Re-swept a third time at `81a2937` (delta report `.scratch/sweep3/report.md`, same hash-diff method: 5 of 34 scenes moved, 29 carried their verdict unchanged) and the A4 LOW batch closed — the Add-link dialog's ~50 px dead zone (the popup height double-counted its own layout padding, so the VerticalLayout spent 32 px of slack as one gap: card 174 px → 141 px, gaps back to the designed 12/16 px, diff bbox exactly the old popup), mid-word snippet elision (both snippet builders now drop the partial word at each edge and leave CJK alone; the shot exercises the repo-less local path, which is why an FTS-only fix would have left the PNG byte-identical), the background-colour swatch rings, the notice band that had pushed the frameless title bar off the window edge (now y=40..69 under the 40 px bar), and the `recovered` fixture's invented copy citing `appdata/quire.db.bak1` — a path shape that predates ADR-0020 — which now mirrors the string `main.rs` really shows. A third of my own framings died in that re-sweep: the notice band does not "clip the sidebar's last row", because the tree list folds mid-row in the default scene too. A4 tail batch closed 2026-09-21 (ADR-0033, ADR-0034): the empty state's MEDIUM was worse than its copy admitted — a page with no blocks has nothing for any insert command to anchor on (every one of them takes a `BlockId`, and neither `create_page` nor `open_page` seeds a row), so a brand-new page could not be typed into at all. `Command::AppendBlock` + `AppState::start_page()` let the page write its own first paragraph, from a click on the empty panel or Enter in the title, one undo step, refused for container kinds. The two LOWs beside it are fixed: modals (delete confirm, Settings, Add link) now sit on `Colors.scrim` while anchored popups deliberately stay undimmed — the rule is in `docs/UI_ARCHITECTURE.md` §"Overlay layering" — and Toggle Sidebar moved to Ctrl+\, so Ctrl+B means only bold; Settings lists the chord now, which also retires that dialog's stale "falls below the bottom edge" limitation. The palette dispatch got the durable repair this section asked for: `palette_action()` + a no-wildcard `match` + a test over `mock_commands()`. Re-swept against `.scratch/sweep12` into `.scratch/sweep13` (44 scenes): 38 byte-identical, and each of the 6 that moved has a bbox that explains it — `empty` 724 sampled px inside x 506..1032 / y 456..466 (the copy line only), `palette` 7 px (the new hint chord), `link`/`dialog`/`settings`/`dark-link` whole-window (the scrim; `settings` is additionally one ShortcutRow taller) — plus a `--scene empty --click 640,458` shot that renders the page title and one focused empty row where the panel used to be. No RAM bench owed: this batch adds no block kind, no model field and no per-row state. The A4 HIGH closed 2026-09-21 (ADR-0041): a paragraph carrying inline marks lost its word wrap and was clipped mid-word, while the identical unmarked paragraph wrapped. Slint `Text` has no inline formatting, so marks can only paint as separate items and an item cannot break — the fix cuts every *unmarked* stretch to one word per run (`build_runs`) and lays the runs of all three run-drawing delegates (block, table cell, column line) in a wrapping `FlexboxLayout`, so the breaks land where the words do. Marked stretches stay atomic, so two shapes still clip (a phrase longer than the line; a marked line needing more lines than the same words unmarked, the row height being measured from plain text) — the wall moved and is now the narrow case, and `docs/EDITOR_ARCHITECTURE.md §"Platform wall"` records exactly what is left. It also paid a harness debt: scene D had no marks at all, so the RAM gate was blind to the change; `--marks N` and a `marked=` line in `--dump-state` fix that, and the gate reads 1.031× / 1.023× across two batches. Still open on the A4 list: 1 MEDIUM (find counts 16 matches and paints none of them on a blurred block — its selection-based hit routing is the same documented wall, so this one needs a focused manual look before it is called a defect), the rest of the LOW bundle (dimmest-text hint rows in both themes, weakest colour pairs) and one known limitation (`renderer_name()` and `renderer_id()` are compile-time, so a `--features software` shot labels itself "FemtoVG · GL" — deduped to one function this round, but the feature ordering still lies for any build that picks its backend at runtime); menu-overflow polish (was the same root cause as the Move-to HIGH — the popup height clamp landed with it); Explorer drag-and-drop import is deferred by a platform limit (Slint 1.18 delivers no external file-drop events — revisit on upgrade or via a Win32 `IDropTarget` in `platform/`) |
| M9 Android | Shared core model + schema, adapted navigation/IME test plan (starts only after Windows is stable) | 🔄 **M9.pre delivered 2026-09-23** (boots on a device, commit `dc2399f`) · **M9.a delivered 2026-09-23**: the editor's hover chrome is reachable on touch — long-press on a row opens the ⋮⋮ menu through the desktop's own callback, a touch-only "Insert below" row carries the "+" half, and the three row menus stand at 44 dp in touch mode with every Rust anchor multiplying the same number (`ADR-0097`; desktop baseline provably unmoved — 131 shared sweep scenes byte-identical against a `dc2399f` control build). The shell now compiles *and links* for `aarch64-linux-android` + `x86_64-linux-android` (`ADR-0095`), the file dialog and the data directory sit behind `src/platform/`, and the mobile shell is one adaptive UI on `UIState.touch-mode` rather than a second tree (`ADR-0096`). Still owed by a finger: the long-press gesture itself and the IME spike (`M9.0`) — see the delivery section of `docs/ANDROID_NOTES.md`. Before that it was ⏸ parked by explicit user decision 2026-09-20 ("安卓先不做"), and still blocked on M8 shipping Windows-stable. Evaluated 2026-09-20 by measurement rather than reading (evidence `.scratch/m9/report.md`, corrections folded into `docs/ANDROID_NOTES.md`): Slint 1.18's `backend-android-activity-06` + its Android renderer + bundled SQLite **compile and link for `x86_64-linux-android` on this machine's NDK 30** — a Quire-shaped `.slint` (window + `TextInput` two-way binding + popup) checks, and the `cdylib` links in 70 s warm. The one hard blocker in our graph is `rfd`, which has no Android backend and fails to *compile* (2 call sites, `controller.rs:1776`/`:1791`); the renderer there is **Skia/GLES, not FemtoVG** (Slint cfg-gates femtovg off Android), which made the desktop skia baseline in `docs/PERFORMANCE.md` a prerequisite rather than a curiosity — it is now measured, and note it is the **skia-on-OpenGL** arm that produced it, because the default desktop skia build's notifier never fires; the only environment read in the crate is `%APPDATA%`, whose fallback is a cwd-relative path, so an Android build without a supplied data dir runs silently in memory. UI cost measured: 31 hover affordances, 8 popups, 68 `UIState` properties, and row heights of 28–40 px everywhere except the 52 px search result row — that one row is the only interactive element meeting the 44 dp touch minimum. Residual risk is concentrated in upstream Android text input (11 open `a:platform-android` issues, incl. #9240 SwiftKey and #11810) and in **there being no device**: `adb devices` is empty and the local SDK has no `system-images/`. Recommended order is M9.pre (cfg-gate `rfd`, add `platform::data_dir()`, fix the feature list) → M9.0 input spike on hardware → M9.1 read-only viewer, i.e. the viewer is demoted from first because the desktop can now prove the part it was meant to de-risk. **M9.pre landed on 2026-09-23, in that shape:** `rfd` is a desktop-only target dependency behind `src/platform/picker.rs` (7 call sites, and Android answers "cannot open/cannot save a file yet" in the notice bar instead of a dead button), the data directory arrives from the Activity through `platform::data_dir()` with `quire-core` untouched, the feature list is per target with exactly one flag left to type (`--no-default-features`), and the mobile shell is the desktop components read through `UIState.touch-mode` (ADR-0095, ADR-0096). The same day it packaged: `target/release/apk/quire_shell.apk` (named after the library target, like the `.so` inside it), signed, both ABIs inside, 24.29 MiB (≈12 MiB per ABI compressed, 31.3 MiB installed) — the sizes and the build costs are in `docs/PERFORMANCE.md`'s M9.pre section, and the sweep figure behind the 44 dp line is 186 sub-44 px height literals across 28 files, 163 of them in the 3–43 px band (`(min-|max-)?height: [0-9]+px` over `ui/**/*.slint`). What that order still cannot prove from this machine is unchanged: M9.0 is the input spike on hardware, and the 44 dp / hover sweep of `ui/` sits behind it. |
| M10 Block kit A+B | image (paste / insert / attachments / width steps), file + PDF thumbnail, simple table, toggle, columns | 🔄 in progress 2026-09-20 · SPEC §三十七 批次 A+B. Delivered: **toggle**, **image**, **file**, **table** and **columns** — 批次 B is closed. toggle — Schema v6 (`blocks.folded`, view state not content, so it is undoable through `Command::ToggleFold` and never bumps a document); the folded subtree produces **no realized rows at all** (`project_blocks` filters through `visible_block_indices`, the single seam shared with the new `drop_index_for_row`, so the §八 drag landing translates row→model instead of drifting); only a Toggle draws the chevron, so `SetBlockType` away from Toggle re-opens the section rather than stranding its children with no way back; `SplitBlock`/`DuplicateBlock` force `folded: false` because neither copies a subtree; Markdown export degrades to a quote line (the exporter is flat and has no subtree to assemble). image — Schema v7 (`attachments` table + `blocks.attachment`, no FK by design so a dangling reference loads as a missing picture instead of failing the library, + `blocks.img_percent DEFAULT 100`); bytes live in `<library>/attachments/<id>.<ext>`, extension and MIME sniffed from the content not the file name; anything over `MAX_EDGE = 1280` gets a second downscaled `<id>.cache.png` and the UI is only ever handed that copy, so viewing never degrades the original; the decoded rasters sit in an `AppState` LRU capped at 32 MiB weighted by RGBA bytes, because Slint's own path cache is 5 MiB — one photograph — and would re-decode every frame of a scroll; rows reach it through `image-for`/`image-aspect` **callbacks**, so cost tracks the viewport, not the document; undo drops the reference and never the bytes (ADR-0029). file — **no migration**: the kind reuses v7's `attachments` row and `blocks.attachment` column exactly, so `user_version` stays 7; `import_any_file` `fs::copy`s the picked file to `<library>/attachments/<id>.<ext>` and records only its length — no decode, no `image` call, no size cap, so a 2 GB attachment costs the same working set as a 2 KB one until the user asks for it (ADR-0030, §二十二); the row paints name (extension kept, unlike a picture's stem) + a right-aligned size from the `attachment-size` **callback** rather than a model field, and two always-drawn buttons, Open via `platform::open_with_default` (hand-declared `ShellExecuteW`, because `explorer.exe <path>` returns 0x1 even when it launched) and Save-as via `rfd` + `export_to`; Markdown writes `[name](quire://attachment/<id>)` — the *link* form, so unlike a picture the reference survives an export/import round trip. table — Schema **v8** (`blocks.columns`, 0 = not a table); the cells are `table_cell` **child blocks** in row-major order, so the row count is *derived* (`cells / columns`) and adding a row is `columns` ordinary `BlockInserted` changes — one undo step, no table-specific history (ADR-0031). The grid is one delegate: `visible_block_indices` hides cells the way ADR-0028 hides a folded subtree, so a table costs one realized row however large it gets. Tab / Shift+Tab walk the grid and a step past the last cell appends a row; the hover strip adds and deletes at the caret's row/column, and the plan refuses to take a table below 1×1 or to edit a ragged grid; Turn into runs both ways (the line's words land in the top-left cell, flattening gives every cell back as a paragraph); Markdown exports GitHub-flavoured tables, and import deliberately leaves `|a|b|` as text. Gate per kind: all six wiring points land (types / storage kind string / Markdown in+out / Turn into / slash / screenshot scene) and the 10 000-block scene stays ≤1.2× the current RAM baseline — re-measured at this commit (raw rows `benchmarks/results/2026-09-20-m10-image-ram.jsonl`): A idle 116.5–116.6 MB WS / 88.1–88.9 private, D10000 idle 126.4–126.5 / 100.9–101.3, i.e. **1.02× and 1.04× the M7 matrix rows** (114.3 / 88.2 and 121.5 / 97.4), idle CPU 0.2–0.59%. This measures the no-picture regression baseline — it proves the v7 columns and the cache fields did not make everything else dearer, not that pictures are cheap; a 10 000-block page **with** pictures scrolled is still unmeasured, and the 32 MiB decode ceiling is a construction bound plus a unit test, not a reading **(that debt closed 2026-09-21 by ADR-0036, below)**. The `file` slice re-ran the same two arms (raw rows `benchmarks/results/2026-09-20-m10-file-ram.jsonl`): D10000 126.5–126.6 / 101.1–101.3, i.e. the image batch's number to within 0.2 MB, and A 119.2–120.7 / 91.7–92.5 — 1.05×, a +2.6…4.1 MB shift on the empty shell that neither run contains an attachment to explain, so it is recorded unattributed in `docs/PERFORMANCE.md` rather than claimed away. The `table` slice ran the same two arms on the shipped build (raw rows `benchmarks/results/2026-09-20-m10-table-ram.jsonl`): D10000 130.8–131.0 / 104.5–105.7 = **1.08×**, A 124.8–125.3 / 96.7–98.3; both arms sit ~4–5 MB above the `file` batch's, by nearly the same amount, which is recorded as session drift rather than as the table's cost (an empty shell contains no grid). It also carries the one attributable A/B in the batch: guarding the row's `table_cells` model on `kind == Table` — instead of scanning the page and allocating an empty `VecModel` for all 10 000 rows — took D down ≈2 MB WS / ≈2.5 MB private while leaving A flat, which is the control that says the saving is per row and not per session. columns — **no new migration**: the layout stores its box count in v8's `blocks.columns`, a box is a `column` child block and a line in a box is that box's child, so undo, redo and §十八 need to know nothing about layouts (ADR-0032). Slint has no recursive components, so the layout's content is drawn inside the layout's **own** delegate — `visible_block_indices` hides boxes and their lines, which is what §三十七's "分栏只在可见窗口内展开" asks for and what §十二's virtualization premise requires; the delegate gets one flat `[ColumnItem]` plus a `[ColumnBox]` shape table it slices, because QOML cannot filter a repetition. Tiling is `FlexboxLayout` (`no-wrap`, `alignment: stretch`, one `horizontal-stretch` per box), not hand-written widths; the hover strip adds a box at the right end and deletes from it, refused below two and above three, and a deleted box's lines re-key onto the box before. An empty box is the one thing a click cannot put a caret in, so it says so and `ColumnsAddBlock` gives it its first paragraph. Markdown flattens the layout to page-level paragraphs (words and reading order kept, shape lost). Two things this slice fixed on the way, both shared with the grid: `InsertBlockAfter` used to force `parent: None` and key off the container's own row, so a "+" or "Paste below" could wedge a top-level block inside a container's subtree — and a page is one flat slice sorted by `order`, so that breaks the subtree contiguity every index reader depends on; and the "/" popup anchored itself with a hardcoded 350 px window reserve, which the menu outgrew the moment it gained a kind, so it now measures its own height like the "+" menu already did. Gate re-measured on the shipped build (raw rows `benchmarks/results/2026-09-20-m10-columns-ram.jsonl`): D10000 135.5–137.0 / 109.9–110.7 = **1.13×**, A 126.5–127.0 / 98.4–99.6 = 1.11× — inside the ≤1.2× gate, and the fourth batch running the same direction on both arms, which is now written up as a method finding: the only per-row cost this kind can add to a page with no layout on it is two 8-byte model pointers (`ModelRc::default()` is `None` in Slint 1.18 and allocates nothing), ≈160 KB across the bench page against the ≈3.5 MB that D − A grew, so the drift stands unattributed and the next kind owes a same-session control build. The sweep grew to 44 scenes (`.scratch/sweep12`): `columns` and `columns-3` are the tiling pair, 1 111 differing pixels all inside the layout's own band, and the three menu scenes moved because the list gained a row. Remaining: 批次 A's PDF first-page thumbnail (deferred by explicit user instruction 2026-09-20, and the built-but-uncommitted version of it was dropped by the same user on 2026-09-23 — the route is **no longer undecided**: four routes were measured and the pure-Rust one chosen, with its bounds, in `docs/REPORT_TRACK4.md` §T4.1, which is where ADR-0080's vacant number waits — until it lands a `.pdf` and a `.zip` differ only by name). Orphaned-attachment GC was the other open item and it is **delivered 2026-09-21** (ADR-0037): Settings → STORAGE → **Reclaim** deletes the `attachments` rows nothing reaches — every page's blocks, both undo stacks up to the 100-step cap, the copied block — then their files, and it deliberately never lists the folder, because a session whose attachment load failed would otherwise "see" zero references and delete the library. 批次 A's clipboard-bitmap paste is delivered 2026-09-21 (ADR-0035): Ctrl+V with a screenshot on the clipboard stores it through the same `import_bytes` path as a picked file, `CF_DIBV5`/`CF_DIB` decoded by `platform/dib.rs` — hand-declared FFI per ADR-0025, the decode kept out of the Win32 file so it is testable without a clipboard — text outranking image when a copy carries both, and an empty block *becoming* the picture rather than leaving a stray line. No RAM bench was owed for it (no new kind, no model field, no per-row state) and the sweep moved 0 of 44 scenes; what it did measure is the paste's own latency, in `docs/PERFORMANCE.md`. **ADR-0036** then paid the picture-scroll debt: `--pictures N` turns every `rows/N`-th bench row into an image block over a pool of 200 generated 1280×720 fixtures seeded only on a fresh library (so a measured pass loads rather than re-encodes), and the app reports its own cache on stderr under `--dump-state`, because a working-set counter cannot see a cache whose unit is one raster. Readings (raw rows `benchmarks/results/2026-09-21-m10-media-ram.jsonl`, nine scenes one sitting): the 32 MiB ceiling **binds at 9 rasters / 33 177 600 bytes (98.9 %)** in every scrolled media arm whether the page holds 500 or 5 000 pictures; 500 pictures on a page you never scroll to are indistinguishable from none (0 decodes, D+500 = D); and a picture *on screen* costs ≈9 MB of process memory against its 3.7 MB raster — the viewport is the binding constraint, not the cache, so the ceiling stays where it is. The gate itself re-ran same-session: D 1.12× / A 1.10×, with the media arms deliberately outside it. **And it invalidated the harness:** Slint's list content offset is negative going down, so scene F's timer had been adding into a clamp ever since M2 — the continuous-scroll numbers on record measured 60 property writes a second at offset 0, and the `--scroll-y` pixel control proved it (`0` and `+2000` byte-identical PNGs, `−2000` differs). Real scroll is now 36.5–38.4 % of a core at a wheel tick and 83–88 % at a flick, and every pre-2026-09-21 scroll number in `docs/PERFORMANCE.md` is marked for re-reading. Next is 批次 C (highlight / bookmark / embed / math / TOC / synced block), per SPEC §三十七. The same day, `benchmarks/scripts/scroll_ab.ps1` paid the follow-up the fix created: the corrected scroll measured against **both renderers in one sitting**, each binary pre-flighted against its own reported renderer so a skia build that kept the femtovg default cannot pass for an A/B (rows `benchmarks/results/2026-09-21-m10-scroll-skia.jsonl`). skia/GL is still cheaper to scroll — ≈18 % at a wheel tick, ≈21 % at a flick, not the third the M7 row claimed — for ≈+34…43 MB working set, so **femtovg stays default** and the verdict's skia sentence is now proven rather than inherited; the 9-raster ceiling came out renderer-independent (identical `peak_bytes` on both arms), because the LRU sits above the renderer. Reproducing those rows through the matrix is what caught a third instrument bug: `-Only A,B` reaches a `[string[]]` parameter as **one** string under `powershell -File`, so a multi-token filter matched no scene, wrote no rows and exited 0 — it now splits the tokens and throws on an empty match, and the matrix gained the two shapes the batch had run by hand (the flick on a text page, a short page of pictures) so every published row is reproducible from the script. One defect the table slice surfaced and fixed on the way: `DocumentRow.head` bound its `height` and left `y` alone, and Slint centres such a child vertically, so the page title had been rendering ~34 px below its binding in every scene since M1 — with a tall first block it was ~60 px and the grid painted over it. That fix re-baselined 37 of the 42 sweep scenes (`.scratch/sweep10`, all 37 diffs confined to the title band), and the trap is written down in `docs/UI_ARCHITECTURE.md` §"Slint geometry traps". Can interleave with the M8 tail; M9 stays parked. |
| M11 Block kit C | code highlighting, bookmark (withdrawn, ADR-0081), embed card, math, TOC | ✅ 2026-09-22 · SPEC §三十七 批次 C — closed. Delivered: **math** (ADR-0038), **TOC** (ADR-0039), **embed** (ADR-0040) and **highlight** (ADR-0042). math — `BlockKind::Math` (row id 20) and `MarkKind::Math`, both storing LaTeX-subset **source** and rendering it to Unicode through one function, `core::math::to_unicode`. **No migration** (kinds and mark kinds are strings in the database, so `user_version` stays 8) and no layout engine, which is what keeps §三十七's "要引入排版引擎必须先出 ADR 并附内存数字" gate untriggered. The block row asks for its glyphs through a `pure callback math-render`; the inline runs convert at projection time instead of in a binding, because a binding pays per repaint — and the block's binding is kind-guarded (`is-math ? … : ""`), because `visible: false` does not stop a binding from running: ≈0.61 µs per formula measured, so an unguarded row is ≈6 ms per projection on a 10 000-line page. The renderer's contract is three rules with tests on each: the output never loses what the user typed (an unknown command and a `\begin{…}` echo their own source), whitespace in the source is content and not syntax, and the render is idempotent. Markdown: `$$ … $$` fences verbatim the way code fences are, `$…$` behind TeX's flanking rule so "costs $5 and $10" stays prose, and the exporter escapes a `$` only when a pair could really re-form on the way back. Ctrl+M marks a selection in all three editors (block, table cell, column line), `$$ ` converts a line, the kind is in slash / Turn into / the "+" menu, and the settings dialog lists the chord. Gate: **1.016× the control's private bytes** on scene D — and this is the batch that pays 批次 B's debt: the previous commit built in a clean worktree, arms alternating in one sitting, each identified by md5 and size (raw rows `benchmarks/results/2026-09-21-m10-math-ram.jsonl`). The sweep grew 44 → 46 scenes (baseline `.scratch/sweep21`): `math` and `math-inline` added, 4 existing scenes moved with a bbox that explains each one, and `math-inline` caught a real defect on its first shot — the run `HorizontalLayout`s defaulted to `alignment: stretch`, so a marked line with leftover width painted its three runs ~155 px apart; `alignment: start` on all three run rows moved **0 of the 44** baseline scenes, because every one of theirs overflowed instead. Not done and not pretending: no display-style layout (`\frac{a}{b}` is one-line `a/b`), no environments, no math font, and no headless scene of a formula *being edited* — that and Ctrl+M need a human window. TOC — `BlockKind::Toc` (row id 21, database string `"toc"`) stores **that it is a contents block** and nothing else: `toc_entries()` rebuilds the list from the page's headings on every projection, walking the same `visible_block_indices` list the projection itself produced, so a heading behind a fold or inside a container is never offered as a link and renaming a heading renames its line — there is no copy that could go stale, and **no migration** again (`user_version` stays 8). The row is selectable but not editable, like a divider. Clicking a line fires `UIState.toc-jump(int)`, which reuses M8's `quire://block` anchor path: it moves the caret and, the honest limit shared with every anchor in the app, does **not** scroll the viewport — Slint 1.18's plain `ListView` has no `bring-into-view` (that function is on `StandardListViewBase`) and nothing in core moves a Flickable when a child takes focus, so a variable-height `reveal` is its own future slice that would fix both. Markdown is one marker line, `<!-- quire:toc -->`: writing the list instead would put this library's block ids into a foreign document, and the marker renders as nothing anywhere while reading back as itself. Gate: **1.007× the control's private bytes** on scene D (control `cc7ccf0` from a clean worktree, md5 `a81ce6df…` / 21 928 960 B, against this tree's `f6d9a576…` / 22 017 024 B; rows `benchmarks/results/2026-09-21-m11-toc-ram.jsonl`) — and the 0.75 MB gap is *inside* the control arm's own 1.6 MB spread, so the reading is no measurable cost rather than a small one. Sweep 46 → 47 scenes (baseline `.scratch/sweep22`): `toc` added, 3 menu scenes moved inside their own bands, 43 byte-identical. embed — `BlockKind::Embed` (row id 22, database string `"embed"`) keeps the **address** in `blocks.text` and derives the card's two lines while drawing, through two pure callbacks (`embed-label`, `embed-url`) over `core::embed::describe` / `with_scheme`, so there is no second copy of the link to fall out of step and **no migration** for the fifth kind in a row (`user_version` stays 8, now 23 kinds in `BlockKind::ALL`). SPEC §二/§三十三 rule out a WebView, so the card plus one button that hands the address to the system *is* the feature: 21 named providers, Google read as its products from either the subdomain or the first path segment, an unlisted host shown as itself, and `Embed` / `Link` / `Email` for the shapes with no host to name. Markdown is the bare address on its own line — the one shape a GFM renderer already turns into a link — and import accepts only a single token carrying an explicit `http(s)://`, with a control test that an address inside a sentence stays a sentence; the address is exported verbatim rather than through the mark renderer, because a url is full of `_ * ~ &`. The slice also put a scheme allowlist in front of `open-link`'s shell branch: a compiled probe showed std quotes the `cmd /C start` argument, so the injection suspected there was never real, but `start` will *run* a path, a UNC share or an installed protocol as readily as it opens a url and a link target is data that arrives from a file — so `file:///…`, `\\share\x`, `javascript:…` and a bare `C:\…\calc.exe` now do nothing at all. Gate: **1.005× the control's private bytes** on scene D (control `4fa2b7b` from a clean worktree, md5 `9c5e153f…` / 22 017 024 B, against this tree's `19aea07c…` / 22 072 320 B; rows `benchmarks/results/2026-09-21-m11-embed-ram.jsonl`) — a 0.55 MB gap between the two steady means against the control arm's own 1.2 MB spread, so again *no measurable cost* rather than a small one. Sweep 47 → 49 scenes (baseline `.scratch/sweep23`): `embed` and `embed-empty` added, 3 menu scenes moved inside their own bands with `dark-slash` landing on `slash`'s box to the pixel, 44 byte-identical. Not verified by this build: the browser actually coming up, the card mid-edit, the arrow's hover state and the dark-theme card — no headless scene presses a TouchArea. Between them, 2026-09-21 closed M8/A4’s last open HIGH (ADR-0041): a marked line now wraps at word boundaries in all three run-drawing delegates, which was thought to be the wall highlight was waiting behind — a per-token colour run can now break. **highlight** landed after it and took a different route: a token is not a word (a string literal spans lines), so the run channel cannot carry a per-token colour after all, and `blocks.lang` is Schema **v9** (`add_lang_column`) — the one migration of this batch, because the other four kinds stored nothing new and this one has to remember which lexer to run. Slint's `Text` paints one colour, so a coloured block is **six** `Text`s of the same string stacked glyph-for-glyph: `core::highlight::layer(text, lang, frame, advance, kind)` returns the whole block with every character of a different kind blanked to `U+00A0`, plus hard newlines at a column budget `floor(frame/advance)`, and six equal-length strings cannot drift line from line — the layer that measures the row (`kind 0`) is one of them, so no second layout has to agree with the first. One invisible `Text` probe measures the mono advance once for the app; the language arrives through ⋮ → Language (undoable `Change::BlockLangSet`, `CODE_LANG_BASE` submenu) and travels as a fence info string both ways (` ```rs ` in, ` ```rust ` out, rich paste keeps its `lang`). Two defects only a screenshot could catch, both recorded in ADR-0042 and in the geometry traps: a `Text` with no bound `width` never wraps, so the five layers broke only at the lexer's own newlines while the measuring layer soft-wrapped — one line of drift per long line, caught on the first `code-hl` shot and re-proved at 760 px where the fixture really breaks; and `visible: false` does not stop a binding from running, so five lexer calls fired on every repaint of every row of the page until the layer Rectangle became a conditional element. Gate: **1.008× the control's private bytes** on scene D against a `2b62498` worktree control, with a third arm — same candidate exe, `--code 0` — so the tree's cost and the colouring's cost are separate readings; the coloured arm came out *below* both (0.924×) on a fixture whose rows are longer and taller, so that 7.5 MB is published as unexplained committed-not-touched arena (working set flat at 132.2–133.4 MB on all three arms, sign flipping across 200→5 000 coloured rows) rather than as a saving — what the gate stands on is that **25× more coloured rows make the process no bigger** (rows `benchmarks/results/2026-09-21-m11-highlight-ram.jsonl`). SPEC §三十七's actual requirement is latency, and scene E answers it: handler median **89 µs** with 2 000 coloured rows against 92–112 µs without, p95 154–167 against 154–207 (`benchmarks/results/2026-09-21-m11-highlight-typing.jsonl`). Sweep 52 → 54 scenes (baseline `.scratch/sweep26`): `code-hl` and `dark-code-hl`, **0 of the 52 existing scenes moved**, and the two new PNGs are checked by a colour census for all five token colours — the one gate that would have caught a highlighter that painted nothing while every other number stayed right. Remaining in 批次 C: **none** — `synced block` landed with M13 (ADR-0052: one block holds the content, `blocks.sync_ref` is the mirror's only fact, cycle detection at write time), and `bookmark` was **withdrawn 2026-09-22, not deferred** (ADR-0081): the card shape it would have drawn is already the embed card's (ADR-0040), and the one part missing — a fetched title and favicon — is precisely the part that needs a TLS client and a consent decision Quire has not made. Hard limits: no JS/WASM runtime, no WebView (SPEC §二/§三十三), and the one requirement 批次 C carried in writing — that highlighting must not make long-code-block typing latency measurable — is met, 89 µs against 92–112. |
| M12 Page appearance | icon, cover, font (default/serif/mono), full width, small text, lock, version history, templates | ✅ delivered 2026-09-22 · SPEC §三十八 is done: six slices. Its schema steps are **v10, v11 and v21–v23**: the database layer claimed 12–20 from the same base, so in the merged chain the cover / lock / template steps are the ones at the end — see the note at `CURRENT_VERSION` in `src/storage/migrations.rs` for why that side moved and not the other. Delivered: **页面版式** (ADR-0044) — the three switches, and the schema step they needed. Schema **v10** (`add_page_appearance_columns`) adds `pages.font TEXT NOT NULL DEFAULT ''` and `pages.layout INTEGER NOT NULL DEFAULT 0` (bit 1 = full width, bit 2 = small text), each guarded on its own absence, so a v9 library's every page reads back exactly the look it had — which is what `the_v10_step_adds_the_page_look_to_a_v9_database` insists on rather than assumes. `font` is a string for the same reason `blocks.kind` is: an integer code makes a reordering of the enum a silent re-pointing of every existing row, while `PageFont::try_from_str` folds `"comic sans"` to `Default` so a page written by a future build opens as an ordinary page instead of as an error. `layout` is one column because the two switches always travel together — `Change::PageLayoutSet` carries both, the menu writes both, the reader tests two bits — where two BOOLEANs would mean a second conditional `ALTER` in a step that already has one per column. Rendering goes through one new global, `PageType`, which *derives* the document tier from three `UIState` properties: **119 call sites** across four `.slint` files moved from `Typography.*` to `PageType.*`, chrome (sidebar, menus, palette, settings, and a block's own caption line) still reads `Typography`, and **no block gained a field** — §三十八's "不得下沉成 per-block 字号" holds by construction, not by discipline. Small text is one factor, **0.87** (= 13.5/15.5, this app's own code:body ratio), applied to the whole tier including headings; the line-height tokens stay unscaled on purpose, being factors of a natural line box that already shrinks with the face. Two seams needed a second look: marked runs name an italic *family* rather than setting `font-italic`, so a serif page would have italicised its emphasis in Segoe while everything else was Georgia (`PageType.italic-font-family`); and the app-wide `code-probe` that measures the monospace advance now reads `PageType.size-code`, so ADR-0042's colour layers stay measured against the glyphs they colour. Entry point: 顶栏 ⋯ → **Style** submenu (Back, three fonts with a check on the live one, Full width, Small text), deliberately **not undoable** like the Favorite above it — a look is a property of the page, and Ctrl+Z on a page you were typing in must not be a font. `duplicate_page` carries the source's look, which caught a real bug: `workspace.duplicate` created the copy with defaults while the persisted `PageCreated` carried the source's, so a duplicated page read differently before and after a restart. Gate: **no RAM bench owed**, and the reason is the shape — a row reads a global property either way, so the derivation adds one multiply at the global and nothing per row; the pixel evidence is the substitute and it is the strong kind, **56 of the 57 existing scenes byte-identical** with `menu.png` the only mover at 1 430 px inside x 240..423 / y 542..779 (the submenu's own box). Sweep 57 → 64 scenes (baseline `.scratch/sweep32`): `page-style` plus the five combinations and a dark arm, each measured against `default.png` with a self-vs-self control at 0 px — serif 63 638, mono 62 780, small 55 285, full width 51 533, all three 68 779, dark 63 140 — and full width is the only one whose bbox starts at **x 284** (sidebar 260 + `Theme.spacing-xl` 24) where the rest start at 390, i.e. the gutter moved, which is the number that separates a layout switch from a font swap. Open after that slice: **lock**, **version history**, **templates**, and **cover** — the last three each still need the `pages` column they ask for (cover / locked; cover got its v12 the same day, below), version history still owes the disk-and-RAM retention numbers §三十八 refuses to leave unlimited, and a locked page still has to prove it never swallows input silently. **2026-09-22 second slice: 页面图标** (ADR-0045) — schema **v11**, one conditional `ALTER` adding `pages.icon TEXT NOT NULL DEFAULT ''`, and the value stored is **the emoji itself, not the picker's index into it**, so growing or reordering `core::icon::PICKER` (96 emoji, 12 rows of 8, `PER_ROW` shared with the grid) cannot silently re-point a page written yesterday. Migration 11 also pulled the guard out of migration 10 into a shared `add_page_columns(conn, &[(&str,&str)])`, so a half-migrated file converges instead of erroring on a duplicate column name. The .slint side names **no emoji**: `fill_icon_picker` copies the catalogue into a `ModelRc` on open, which is what keeps the grid from drifting from what storage accepts. The hard part was reading "未设置时用标题首字符占位" as three rules instead of one, because an empty slot means three different things in three places: **tree rows** take the title's first character (that slot used to hold a glyph that said nothing about *this* page); **Favorites / Recent** show the stored emoji *or* keep their star / clock, since those marks name the section, not the page — hence `icon_slot` and `icon_mark` rather than one function; the **editor hero** draws nothing above an iconless title, because repeating the title's first character at 46px directly above the title is an echo. Two geometry traps surfaced in the pixels: a parent row already spends its 16px slot on the chevron, so the emoji shares that box and hands it back on hover (a second box pushed every parent's label one indent past its own children), and the first pass applied the initial to Favorites/Recent, replacing stars with letters in all 64 baseline scenes. Entry point is 顶栏 ⋯ → **Set icon** (`IcSmile`), which *replaces* the menu with the grid instead of nesting under it; setting and clearing are persisted as `Change::PageIconSet` and are **not** on the Ctrl+Z stack, same as Favorite and Style — a property of the page is not an edit to the document. `duplicate` carries the icon in memory and in the persisted `PageCreated`. Gate: **no RAM bench owed** (one short string per page, two small allocations per sidebar row on a mutation path, a few KB per picker open); the pixel reading is the unusual kind — **64 of 64 scenes moved on purpose**, because every iconless row's slot changed, and the shape is what separates that from a leak: 72 937 px total, modal scene 1 153 px inside x 13..52 / y 365..713, and **0 px past x 53 in 63 of the 64**, `menu.png` alone at 345 px for its own one-row-taller popup. Sweep 64 → **67** (baseline `.scratch/sweep33`): `page-icon`, `icon-picker` (deliberately over an iconless page — the state a user is in when they reach for it) and `dark-page-icon`, the last one because the headless software renderer draws emoji monochrome and the dark theme is where legibility gets answered. **Hand-test owed**: the GPU renderers' colour-emoji path was not measured. Still open at the end of that slice: the hero icon is display-only (no hover "Add icon"), and cover had not been touched yet — it landed the same day, below. **同日补记（ADR-0046）**：§三十八 icon 原文的「+ 本地图片」那一半决定**不做**——附件只有一档 `MAX_EDGE = 1280` 的降采样，16px 的图标槽要么让每次侧栏重建去解一张 6.5 MB 工作集的图，要么加第二档缓存，而第二档必须连 M10 回收扫描的引用者一起改（icon 是**页**指向附件，不是块），否则用户的图标会被当无人引用的文件回收掉；「放一张图」的需求由 cover 承接。 **同日第三切片：封面 cover**（ADR-0047）——schema **v12**，一列 `pages.cover INTEGER NULL`（可空是这个特性的全部重点），存 **AttachmentId**。可空而不是 `DEFAULT 0` 是被 `0` 逼出来的：`0` 是一个合法的附件号，所以「没有封面」必须是第三种值（对比 icon 那列的 `''`，那里空串无歧义）。存 id 不存路径是 ADR-0046 花钱买来的那条：§三十七 的回收扫描只能从数据库回答「还有谁指着这个文件」，一列路径是它解不回来的字符串，于是用户自己的封面会变成下一次回收的牺牲品；引用者列表因此加了两条 —— `workspace::cover_ids()` 与块并列进扫描，`Change::PageCoverSet` 回答 `attachment_ids_in`（**一条还没撤销的 undo 步骤也是一个指针**；钉住它的测试删掉一个 image 块、重启会话让 undo 不再投票，然后要求那张图先作为**页的封面**活下来、又在页面松手之后立刻走掉）。渲染：hero 上一条 `Typography.size-cover-height` = 168 px 的带，标题落在带子下缘之内，上面罩一层固定 `#0000009e`。为什么是固定的一层而不是读图：§三十八 要「过 §二十一」且禁「最弱配色」，但应用不可能每次开页都按 hero 尺寸去解一张任意 JPEG，逐图自适应颜色又是 ADR-0039 拒绝给存储的派生值 —— 固定遮罩把要求变成有界算术：α 是一个字节（`0x9e` = 158，α = 0.6196）⇒ 任何像素合成后 ≤ 255 − 158 = **97** sRGB ⇒ 相对亮度 ≤ 0.1195 ⇒ 白字 **6.19:1**，而 4.5:1 那条线自己的门槛是 α ≥ 0.535。最坏情况是一张纯白的图，所以 `create_solid_fixture` 就写这么一张，`page-cover-white` 是它的对照场景；`benchmarks/scripts/contrast_probe.ps1` 从渲染出的 PNG 上量比值，自带 known-answer 三臂（21:1 / 1:1 / 6.19:1）、找不到声称的墨就 exit 2（静默探针 ≠ 阴性）、外加一个人造的 1.61:1 必然失败臂 —— **门禁要能说不，才算说过**。实测 6.19:1（两个主题的白色封面）与 14.04 / 14.09:1（照片）。带高是**派生**的 `max(168px, hero 底 + spacing-md)`，因为 icon 会把整个 hero 往下推，固定带高会让白字直接站在页面背景上——那是遮罩唯一护不住的位置。第一次量图抓到的正是这个漏网之鱼：hero 的 emoji 还在读 `Colors.text-primary`，`#1f2328` 坐在 `#232439` 的照片上 = **1.04:1**（相对亮度 0.0165 vs 0.0191），现在它和标题走同一个条件。入口：页面 ⋯ → **Set cover** / **Change cover**（同一个 id，标签回答「现在有没有」）/ **Remove cover**（只在有封面时存在），跟 Favorite、Style、icon 一样**不进 Ctrl+Z**，`duplicate` 把源页的封面带上。门禁：**不欠 RAM bench**，理由不是形状而是有一个已经存在的上限可指——封面走的就是 image 行那个 `image_for`（state.rs:1898），于是一张 `≤ 1280` 边的显示副本进的是 `MAX_ATTACHMENT_CACHE_BYTES = 32 MiB` 的加权 LRU，「有封面的页」与「顶部放一个 image 块的同一页」等价；`id <= 0` 在碰到存储之前就返回默认值，所以 67 个既有场景一个都不付费，它们的字节相同就是证据。像素读数是这一片最安静的一次：**66 of 67 byte-identical**，唯一动过的 `menu.png` 是 9 个采样像素落在单列 x 414 / y 706..722，即页面菜单从十行变十一行后 **ListView 滚动条滑块**的位置（弹窗在此之前就被 `min(rows*30+8, window-h - menu-y - 20)` 夹住了，多一行剪不掉任何东西）——把它读成「弹窗变矮了」是陷阱，放大图说的是滑块。Sweep 67 → **72**（baseline `.scratch/sweep35`；注意 `.scratch` 按 worktree 各自 gitignore，同名不是同物）：`page-cover`、`page-cover-white`、`page-cover-icon` 加两个 dark 臂，其中 `page-cover-icon.png` 是修复前后**唯一**变化的那张，因此它本身就是 A/B 证据。**Hand-test 仍欠**：GPU 渲染器的彩色 emoji 盖在封面之上（软件渲染器画单色字形，所以那一路才可测）。 **同日第四切片：锁定 lock**（ADR-0048）——schema **v13**，一条 `ADD COLUMN locked INTEGER NOT NULL DEFAULT 0`：「没锁」是一个值而不是一种缺席，所以它不像 cover 那列可空，也因此一步迁移不需要回填语句。门开在**漏斗**上（`exec_editor`，约九十处调用点共用），句子里那四个关闭点读成两层：Rust 拒写，.slint 拒光标（`EditorBlock.editing` 多一项 `!UIState.page-locked`，因为只在命令层设门等于允许一个「按 Enter 才说不」的活输入框在重新加锁之后继续存在）。审计把漏斗外面的入口一个个点名，并且抓到两个**真洞**：`let _ = exec(SetBlockType)` 之后无条件 `clear_block_ref` —— 转换被拒了，引用照清；`duplicate_page_block` 在命令层看到这块之前已经按树造出一个子页 —— 拒掉的复制留下一页孤儿。于是这一片立的那条规则不是「把门设好」而是**跟着命令走的写必须看命令的返回值**，三个漏斗外的入口各自带门，测试除了比块列表还比子页计数（一次让树变肥的拒绝仍然是一次写）。`ToggleFold` 是锁内唯一还执行的命令（§三十七 登记它为持久化*视图*状态），门里写着一个具名例外就得用断言钉住它：其余十条返回 `None` 时它必须还是 `Some`。撤销同样在锁后面，但**拒的不是丢**：锁之前的 undo 栈原样保留，解锁接着用。复制页面**不**继承这把锁，与继承字体/icon/封面正好相反 —— 外观跟着走是因为副本该长成源的样子，锁不跟着走是因为「复制一个 locked 的页」正是用户想改它又不动原版的动作。「不能静默吞输入」落在通知条上，去重依据是**屏幕上那一行字**（bar 是粘的）而不是队列，因为拖拽 hover 每个重绘帧问一次 `can_move_block_to`；hover 自己保持安静 —— 落点线消失就是那个手势的反馈，紧随的那次 drop 才值得一句。⋮⋮ 的编辑项是**删掉**不是置灰（那个菜单没有 disabled 状态可以说「不行」），留下 Copy link to block 与 Copy block 两行只取的。门禁：**不欠 RAM bench**，一 bool 一页、hover 是一次比较而不是一次分配，没有可测的每帧成本；像素读数是这一片的证据：**71 of 72 场景字节相同**，唯一动过的 `menu.png` 是 8 个采样像素落在单列 x 414 / y 692..706，即页面菜单的**滚动条滑块**（弹窗在加这一行之前就已经被 `min(rows*30+8, window-h - menu-y - 20)` 夹住，多一行剪不掉任何东西，能动的只有滑块）。Sweep 72 → **76**（baseline `.scratch/sweep36`；`.scratch` 按 worktree 各自 gitignore，同名不是同物）：`page-lock`、`page-lock-menu`、`page-lock-block-menu` 加一个 dark 臂，每张都另找一张自己的对照来证明画出了东西 —— pill 对 `default.png` 是 244 px 落在 x 390..560 / y 52..74（标题上方那一颗，页面上别的一动没动）；⋮⋮ 对 `block-menu.png` 是 2 101 px，这是一对**干净**的 A/B，因为加锁的那张逐字抄了 overlay 分支（同一个块、同一个 x 320 / y 300），于是差异只剩「八行没了 + 一颗 pill」——它自己对 `default.png` 是 809 px（x 320..560 / y 52..362），未加锁那张是 2 055 px（x 320..494 / y 300..586）；⋯ 那张要老实说：`menu.png` 锚在 106 那一行（y 522..778，被夹住），而 lock 场景开的是**编辑器正在显示的那一页**，弹窗落在 y 266..610，所以它对 `default.png` 是 3 316 px、对 `menu.png` 的 4 586 px 是「两个盒子 + 一个标签」而不是一盒改动。测试：state 两条、workspace 一条、storage 一条 v12→v13 迁移（含 control 断言：`DROP COLUMN` 之后先证明列真的不在，再要求旧库读回 0 且 icon/cover 原样），全库 407 条 `#[test]` = 395 通过 + 12 ignored；并且做了一次变异检查——把门表达式强行短路成 false，两条新测试**必须**失败，能说「不」的门禁才称上过话。 **Hand-test 仍欠**：⋯ → Lock page → 点任意一行 → 看见 pill 与通知条（加上 cover 那两片欠的换封面与彩色 emoji 盖图）。 **同日第五切片：模板 templates**（ADR-0049）——schema **v14**，一条 `ADD COLUMN template INTEGER NOT NULL DEFAULT 0`，形状与 v13 那条一模一样（「不是模板」也是一个值，所以迁移不需要回填语句）。这一片的重点全在**没有造出的东西**上：没有 `templates` 表、没有 `template_blocks` 表、没有模板文件格式——模板就是一张页，那一列是它身上唯一一条普通页没有的事实，于是 §三十八 那句「不得引入第二套内容格式」是被结构满足而不是被纪律维持：marks / 颜色 / `lang` / `columns` / `img_percent` / 一个 `Page` 引用在复制时一律随行，`fill_template` 二十行就够了，因为它 clone 的是行（id 由文档分配器另发、order key 原样留、parent 指针重映射到副本上——把表格的格子与 toggle 的孩子绑在一起的就是这一步）。旗标只有**一条**写入路径：`PageCreated` 带着整个 `Page`，所以没有 `Change::PageTemplateSet`，也没有「把现有页变成模板」的开关——改一个模板的方式是拿它开一页、编辑、存回去、删掉旧的；代价老实写着：保存从不覆盖，所以旧副本会留在库里，而库里可以有两个同名模板（按年龄排序）。「看不见」读成**不挂树**而不是一长串过滤器：`create_template` 既不进 `roots` 也不进任何父亲的 `children`，所有走树的枚举器免费跳过它；四把不走树的门各自补一项（`open_page` 在 `mark_opened` 之前返回，因为打开会写 recents 与 `current-page` meta；`search_index::matches` 的 join 多一个 `AND p.template = 0`；LAN 过滤页列表与子页遍历，且 `/api/page/<模板 id>.md` 答 **404** 而不是它自己的 Markdown）。第五把门考虑过并**拒绝**：不给模板的行建索引——那会让 `insert_block` 去问脚下这页是不是模板（一个 join 已经知道的事实出现第二个真相源），而 `rebuild` 就得与 `insert` 对「哪些行属于索引」持不同意见，于是模板会在一次重建之后开始出现在搜索里；钉住它的测试因此除了断言命中为空还断言 `search_blocks` 的原始行数，好让「看不见」不能被解释成「没数据」。五个预置（`core::template::PRESETS`）是**导入**进库的而不是写进 migration 的（自己写块行得手工对齐 order key、`block_children` 与两张 FTS 表，而菜单 Import 那行调的 `import_template` 已经把三件事做对），两道门各管一个方向：settings 旗标让「删掉五个」活得过重启（否则 Delete 行是谎），名字检查让半途而废的 seed 可重试（旗标在正文之后才记）；`persistence.is_none()` 直接拒绝，所以 headless 场景自己画一份库。插入 = 一步撤销靠 `InsertForest`（十一块的模板按一次 Ctrl+Z 全走，而不是留下九块；产出的仍是普通 `BlockInserted`，flush / FTS / 重启看到的只是「这页长了几行」）；锚点上那行空 paragraph 同批被替换，这只有 `exec_all` 拿前态做计划才成立，而它返回第一个插入块的 id 是因为这条命令可能删掉用户点的那一行。`fill_template` 不产生 undo 步骤（历史栈属于正在打字的那一页，模板页从不开着）。入口是页面 ⋯ → **Templates** 一行（菜单十三行；一行而不是六行，因为那一行的标签就是特性的名字），子菜单标签短是被迫的：全应用共用一个 184px 的 `ContextMenu` 且它的行 elide 不换行，第一版渲染六行里有四行结尾是省略号，于是改成 "Use as new page" / "Save as template" / "Export Markdown" / "Import Markdown" ——把弹窗加宽不在讨论范围内，它会动到应用里每一个菜单并要求重判大半基线。测试顺手抓到 §二十六 通道里一处**真缺陷**（不是模板的）：`export_page` 会把空的列表项整行丢掉，于是「往返」在空行上是假的，而一个空的 bullet 恰恰就是模板的内容；`prefix_lines` 现在为空块写出裸标记（`-`、`1.`、`>`、`#`），这条对普通页面导出同样成立，规则已写进 §二十六 与 export 文件头的布局清单。另两处诚实的账：`delete_page` 回答的是「删掉的是不是屏幕上那页」而不是成没成功（对模板恒 `false`），两条新测试一开始断言错了方向，现在改成前后两次 `workspace.contains()`；回收扫描一项都没加，因为模板的行就是 book 里的块行，`doc.all_blocks()` 已经把它们指着的图算作引用者。门禁：**不欠 RAM bench**，理由是可指认的——一 bool 一页，模板的行是 book 本来就会载入的行，而菜单是一次 popup 填充；像素读数是证据：**74 of 76 场景字节相同**，两个动过的都在页面 ⋯ 菜单上而**同一个菜单在两个锚点上给出两种数字**——`menu.png` 只有 6 个采样像素落在单列 x 414 / y 682..692（弹窗在此之前已被 `min(rows*30+8, window-h - menu-y - 20)` 夹住，动的只有 ListView 的滚动条滑块），`page-lock-menu.png` 是 428 px 在 x 240..422 / y 558..640（那张锚得够高，十三行全画出来，插入点之下的每一行都位移）；把小数字读成「几乎没变」就是这一片要防的陷阱。Sweep 76 → **80**（baseline `.scratch/sweep36` → `.scratch/sweep38`；`.scratch` 按 worktree 各自 gitignore，同名不是同物）：`page-templates`（子菜单）、`page-template-pick`（库选择器，Delete 打开时整列走危险色——这是全应用唯一一处）、`slash-template`（一行命中 "weekly" 的 slash 弹窗）、`dark-page-templates`；标签改短之后重跑一次全量，**78 of 80 字节相同**、只有那两张模板子菜单图动过，这就是「改标签没碰到别的菜单」的证据而不是推断。测试：lib 11 条（`app::state` 8 + `core::command` 的 forest 两条 + `services::lan_server` 一条，种子模板与普通页**只差那一列**，于是那三个断言是三把独立的门）、storage 一条 v13→v14 迁移（含 control 断言：先 `DROP COLUMN` 再证明列真的不在，然后要求旧库读回 0 且 locked/cover 原样）、search 一条（索引里有、搜不到，外加 DROP FTS + `user_version = 1` 的重建臂，两把门各证一次）、markdown 一条（五种空块）；runner 全绿 **421 = 409 通过 + 12 ignored**，`cargo check --all-targets` 零警告。并且做了一次变异检查：把 `template_slash_rows` 的 `.enumerate()` 挪到 `.filter()` 之后（第一版编译不过，改写成能编译的等价错法才跑成），新断言按设计失败并给出 `left: Some((7, "Meeting notes")) / right: Some((8, "Weekly review"))` ——一行模板的 id 必须索引**库**而不是当前可见的那几行。 **Hand-test 仍欠**：⋯ → Templates 的六行全部（含两个 `rfd` 文件对话框，headless 到不了），加上 cover 那两片欠的换封面与彩色 emoji 盖图、lock 那一片欠的 pill 与通知条。 **同日第六切片：版本历史 version history**（ADR-0091）——M12 到此做完，§三十八 不再有剩下的。这一刀**一条迁移都不加**（`user_version` 停在 14）：前五个竖切每刀一列（v10…v14），这一刀是**文件**。§三十八 那句「复用 §二十五 的 snapshot 机制，不另造一套存储」被照字面执行——一个版本就是 `backup::snapshot` 那条 `VACUUM INTO`（同一个 `synchronous=OFF` 交易，同样靠 `Database::open` 读回来验一遍），然后收窄到一页：删掉别的页（`blocks.page … ON DELETE CASCADE` 带走块行 / `block_children` / `marks`）、清空两张 FTS 表、再 `VACUUM` 一次把空间交还文件系统。于是 `versions::read` 用的就是应用开库时那个 `Repository::load`，一个版本**就是**那个格式、无从与格式走散，而 §三十八 禁的「第二套内容格式」又被结构而不是纪律满足了第二次。索引是**每版两行 metadata**：`version/<page>/<created>` 存用户起的名字，`version-files/<page>/<created>` 存那一版指着哪几个附件——第二行不是方便而是必须，因为 §三十七 的回收扫描只查得到的库**活着**的那一份，而「数据库答不出的指针就是回收会删的指针」正是 ADR-0047 立下的账（钉住它的测试先**重启会话**，好证明保住那张图的不是 undo 栈）。 **磁盘与 RAM 的数字**（§三十八 明确不接受无限增长，全部写进 docs/PERFORMANCE.md）：`MAX_PER_PAGE = 20`，每页各算，从最旧那头砍且面板把数字念出来；9.9 MB 的库里一页 60 行的版本 = **122 880 字节（1%）**，5 000 行的 = 790 528（7%），二十版长页 = 15 810 560（**159%**）；120 KB 是**地板而不是内容**（30 × 4096，其中 `blocks` 只占 12 288，其余是 27 条 schema 与每表每索引一页），所以帽必须**按页**而不是按库——一行的页与 60 行的页版本等价，全库共享一个帽会让热闹那页饿死所有安静页。RAM 侧是「一次只有一版在内存里」：一条短暂连接（Quire 不改 `cache_size`，SQLite 默认 ≈2 MB 页缓存）+ 两侧各一份 `Block`（实测 `size_of` = 136 字节，5 000 行 = 680 000 字节）+ `ALIGN_CELLS = 1 << 18` 个 `u32` 封顶的 1 MiB 对齐表。**顺带抓到一处本可以溜过去的缺陷**：清空 FTS 用的 `DELETE FROM search_blocks` 让行数报 0，而 `search_blocks_data` 仍留着 **368 行 / 1.4 MB 的全库用词**——「一页的快照」在字节上仍然是「整个库的副本」，60 行那版因此从 1 560 576 掉到 122 880（12.7×）、二十版长页从 44 564 480 掉到 15 810 560。修法是 `DELETE` 之后再发 FTS5 的 `rebuild` 命令（`delete-all` 更短而这里被 SQLite 拒：只给 contentless / external-content 表用），断言也从「读者会查的那两张表的行数」搬到 `_data` 上——**因为骗人的正是那个行数**。 三个动作落在三个面上（一个 popup 两个视图）：命名是底部输入框（永远在），对比是点一行，恢复是**只在对比之后才出现**的按钮——换掉一整页之前得先看过。行带的是**行号**不是时间戳（Slint 的 `int` 是 32 位而 unix 秒不是，`version_at` 再折回来，被删掉的行返回 `None`）。保存的顺序**本身就是特性**：先 `persistence_force_flush` 再 `VACUUM INTO`，因为那条语句读的是**文件**而不是这一会话的内存，不先刷就得到一个名字叫「现在」、内容是「十秒前」的版本；同秒相撞往前重试四次（≈ 每秒五版的突发上限）而不是覆盖用户亲手打的标签。对比以 block id 认身份、先剪公共头尾（编辑一行只付一次比较），中间段超过 `1 << 18` 就**报成整片重写**而不是算完——多出的行句句为真，而一秒才开的面板更坏。恢复不换文件而走命令系统：一批 `exec_all`（先 `InsertForest`，再逐个删当前的**根**，子树跟着走）= **一次 Ctrl+Z**，新行另发 id、重排 order key，flush / FTS / 重启看到的只是「这页寻常地变了」；**页的名字与长相故意不回滚**（版本是页的*内容*的版本），两道门都是继承来的：`locked_refusal()`（ADR-0048）与「只有屏幕上这页能恢复」。门禁：这一刀**既有磁盘数也有 RAM 数**，因为没有每帧/每行的成本可测——面板是 popup，行在打开时填；像素读数是 **78 of 80 场景字节相同**，两个动过的又是页面 ⋯ 菜单那两个锚点（第十四行 Version history），Sweep 80 → **83**（baseline `.scratch/sweep38` → `.scratch/sweep39`）：`page-versions`（列表视图）、`page-versions-diff`（对比视图）、`dark-page-versions`，而三张图的每一行文字都由运行时那四个投影函数（`version_rows` / `versions_note` / `version_heading` / `version_diff_note`）产出——场景不再抄一份文案，抄本正是会漂移的东西。测试：lib 22 条（`app::state` 12 + 新模块 `core::diff` 10）+ backup 7 条（收窄/读回/同秒拒绝/无文件/索引排序/孤儿清扫/附属文件），`cargo test --all-targets` 全绿 **451 = 438 通过 + 13 ignored**（lib 279+9、integration 159+4），`cargo check --all-targets` 与 `cargo build --release` 双零警告；成本探针 `version_cost` 留成 `#[ignore]`，因为帽是一个会被后来人重量的数字而不是一条断言。 **Hand-test 仍欠**：⋯ → Version history → 打字命名 → Save → 点一行 → Restore → 之后按一次 Ctrl+Z（捕获能画两个视图，但进不了那个输入框，所以「打开即聚焦」与「一次恢复 undo 一步」这两件事仍然只有人手能测）；Templates 那六行（两个 `rfd` 对话框）、cover 的换图与彩色 emoji 盖图、lock 的 pill 与通知条照旧欠着。 §三十八 到此做完：版式、icon、cover、lock、templates、version history 六刀全部落地，M12 的「本阶段不做」之外没有未交付项。 |
| M13 References | @page mention, @date, backlinks panel, synced block | ✅ delivered 2026-09-22 · SPEC §四十 is done: a mention span stores the **page id** and nothing else, so the chip / panel / export read the live title (ADR-0050); @date stores its own ISO text with the format defined once (`core/date.rs`), so it never drifts; backlinks add **no table and no column** — migration 16 is two indexes and the panel is a derived projection (ADR-0051: 78 µs folded against 6 068 µs with the index dropped, i.e. "no full-library scan" is a number and not an intention); a synced block owns no text at all, `blocks.sync_ref` names the source and the row draws that (ADR-0052, and a cycle is refused at the write, not at the draw). |
| M14 Database | record+schema+view model; table → board → list → calendar → gallery → timeline → form → chart; properties, filter/sort/group, relation/rollup/formula, linked database, database templates | ✅ delivered 2026-09-23 · SPEC §三十九 is done in **ten slices D0–D10** (ADR-0060…ADR-0092, schema steps in the v12–v20 range — see `CURRENT_VERSION`'s note for why Track 1's steps moved instead): one `Database` block with **eight layouts** as views of it, 17 property kinds, filter/sort/group/search compiled into SQL so 10 000 rows never get realized (a window costs kilobytes; scrolling an unchanged window recomputes in 29 µs), formula as a self-written lexer + walker with four budgets and cycle detection at save, rollup as a three-name fold over a relation, relation as a **pair written in one batch** (which is what makes a cycle unrepresentable rather than detected), plus linked views and a record template that prefills a new row in one Ctrl+Z. D10 landed the last three user-facing doors (the column type menu, the relation picker, the rollup configurator) and drew the marks instead of typing them (ADR-0092). Timings in `docs/PERFORMANCE.md` `## M14`; per-slice reporting in `docs/REPORT_TRACK3.md`. **Open, on purpose**: the three popups still want a hand in a real window (Escape / click-outside / focus-on-open — the headless sweep cannot reach a callback), and a relation's **fan-out has no cap** (10 000 targets = 28.5 ms vs a 9.1 ms control, 3.12×), which is a product decision rather than a bug. |

## Repository shape (since ADR-0093 + ADR-0094, 2026-09-23)

Two repositories, one edge between them. `quire-core`
(`github.com/PT123123/quire-core`) is the document and database model, SQLite
behind it, and the services that need no window — import, export, search,
attachments, settings, and the LAN framing a sync module will grow out of; it
compiles against `std`, `rusqlite` and `image` and against nothing graphical.
This repository is the Slint shell: `app/`, `platform/`, `ui/`, `build.rs`, the
three binaries, and one dependency on that crate **at a pinned rev**.

That is the prerequisite for M9 and for sync, and it is a *prerequisite*, not
either of them: `cargo check --target aarch64-linux-android` has not been run in
`quire-core` either. Two gate consequences to know before trusting a green run:

- **`cargo test` at a workspace root tests the members only**, and a git
  dependency is not a member. The whole suite is no longer reachable from this
  checkout: what runs here is 134 passed / 10 ignored, and the crate's 431 tests
  run in that repository. Never report a green `just check` as "the app is green".
- Cargo's built-in libgit2 cannot fetch a *public* repository here — it offers
  credentials nobody asked for and takes a 401. `.cargo/config.toml` sets
  `net.git-fetch-with-cli = true`, so `git` does the fetch. `--workspace` is still
  on `just check`'s three lines but is now decoration, not load-bearing.

## Explicitly out of scope for v1

Sync, collaboration, cloud, plugin market, AI, multi-process IPC, custom
TSF/IME implementation, publish-to-site, comments/discussions/reactions.

Everything else the SPEC had parked is now scheduled — SPEC §三十七–四十,
milestones M10–M14 above (decided 2026-09-20, ADR-0027). `image`, `table`,
`toggle` and `database` left this list on that date. `callout`, `Page` and
`Link` were never gaps: they shipped at ADR-0023/0026. Block kinds persist
as strings (`BlockKind::as_str`), 25 of them as of ADR-0052 (`synced`) and
ADR-0060 (`database`), so any kind
numbering in older prose is informal.

## Definition of done (per milestone)

1. Scope items implemented and wired through the controller (no mocks in
   the UI layer).
2. `just check` green: check + tests + release build.
3. Performance re-measured for affected scenes in `docs/PERFORMANCE.md`.
4. Visual scenes re-shot (`just shot`) and reviewed.
5. `PLAN.md` report written; `DECISIONS.md` updated for any non-obvious
   choice.

## M8 verification snapshot (2026-09-21, at `24a3432`)

The A4 tail batch (ADR-0033, ADR-0034) re-ran the gates it could move, and the
one gate the previous snapshot caught itself missing:

| gate | command | result |
|------|---------|--------|
| tests | `cargo test --all-targets` | green: 293 passed, 0 failed, 4 ignored across 10 binaries (+82 over `03e0427`; this batch adds 5) |
| release build | `cargo build --release` | clean, **zero warnings** (re-confirmed by touching the four changed sources and re-checking `--release --all-targets`: 0 warning lines) |
| behaviour | `every_palette_row_resolves_to_its_own_action` | the command-registry test the section below called "still not written" now runs on every `just check` — this is the gate that would have caught `aaa3763`, and the visual sweep demonstrably never was: the whole batch moves 7 sampled pixels in the `palette` scene |
| visual | `sweep.ps1 -OutDir .scratch/sweep13 -Baseline .scratch/sweep12`, then `-OutDir .scratch/sweep14 -Baseline .scratch/sweep13` | 44 scenes: 38 byte-identical, 6 moved, each with a bbox that its own change explains (`empty` copy band only, `palette` hint only, 4 whole-window scrims); the follow-up copy edit then moved **0 of 44**, which is what makes "fixture text no scene renders" a reading rather than a claim |
| not re-run | performance audit, installer, portable | deliberately: this batch adds no block kind, no schema column, no per-row state and no build-profile change, so there is nothing for those gates to observe |

## M8 verification snapshot (2026-09-20, at `03e0427`)

There is no CI on purpose, so this is the manual "definition of done" evidence
for the current head, all of it re-runnable from the repo:

| gate | command | result |
|------|---------|--------|
| tests | `cargo test --all-targets` | green: 211 passed, 0 failed, 4 ignored across 10 binaries |
| release build | `cargo build --release` | clean, **zero warnings** (the four below are gone) |
| visual | `benchmarks/scripts/sweep.ps1 -OutDir .scratch/sweep4 -Baseline .scratch/sweep3` | 34/34 render; **0 of 34 PNGs changed** against `81a2937`, so every verdict carries over: the open list is still 1 HIGH + 2 MEDIUM + 3 LOW + 1 known limitation (`.scratch/sweep3/report.md`) |
| performance | `benchmarks/scripts/audit_results.ps1` | `audit ok`: 12 first-paint + 6 bench batches, every stored summary agrees with its raw runs |
| packaging | `install/verify-installer.ps1` | 4/4 green at this head: 7.94 MB setup, installed exe exits 0, embedded icon still pixel-matches `quire.ico` (384 px sample), `--open` imported pages 14→15, uninstall left zero residue |
| data placement | `install/verify-portable.ps1` | 24/24 green, real per-user library untouched |

### What the previous snapshot was warning about, and what its fix taught

The four build warnings (`unused variable: CMD_COPY_MD` plus two
`unreachable pattern`) were one defect and a functional one: a constant used
in a `match` arm without being imported makes that arm a catch-all *binding*,
so every palette command with an id ≥ 9 — the three Markdown actions and every
"Jump to page" row — ran `copy_current_page_markdown` instead of its own
action, and no page could be opened from the palette. It is fixed (`aaa3763`,
one name added to the import list at `src/app/controller.rs:9`) and the
release build is now silent.

Two things that fix demonstrated about this checklist are worth keeping,
because both are still true. (1) The test suite was green *because* nothing
walks the palette dispatch, so `just check` cannot catch this class of
breakage — the durable repair is a test over the command registry, not the
one-word import. That test now exists (ADR-0034): the ids resolve through
`palette_action()` into a `PaletteAction` enum, the controller's `match` has no
wildcard arm, and a test walks `mock_commands()` asserting every row resolves to
its own distinct action. The same commit that fixed the dispatch changed zero
pixels, so this is the guard and the sweep never was. (2) Neither can the visual
sweep: the CRITICAL dispatch fix changed **zero pixels in 34 scenes**,
including the `palette` scene, because a shot captures the popup's layout and
the bug was in what happens when a row is chosen. A green visual gate is
evidence about drawing, not about behaviour.

### Re-run at `63a4081` (2026-09-21), and one stale verdict retired

| gate | command | result |
|------|---------|--------|
| tests | `cargo test --all-targets -- --skip clipboard_write_and_read_round_trip_unicode` | **374 passed, 0 failed, 11 ignored** across 10 binaries (the skip is the local clipboard being held by another process) |
| build | `cargo check --all-targets`, `cargo build --release`, `cargo build --release --features software --bin quire-shot` | all clean, **zero warnings** on all three |
| visual | `benchmarks/scripts/sweep.ps1 -OutDir .scratch/sweep27 -Baseline .scratch/sweep26` | 54/54 render; **54 of 54 PNGs moved**, every one of them inside `x 96..192 / y 778..784` (plus the Settings row in `settings.png`) — see below |
| packaging | `install/verify-installer.ps1`, `install/verify-portable.ps1` | **not re-run at this head**; the rows above stand as a 2026-09-20 record |

The paragraph above this one claimed a warning still survives in the shot build
(`unused variable: ui` at `src/bin/quire_shot.rs:75`). It does not: `render()` now
takes no `ui` parameter, and the `--features software` build is silent — that
claim is retracted, and the `--all-targets` row above is the evidence.

What *was* still true in the shot build is the labelling limitation the A4 report
filed as "known limitation, not a defect", and it has now been fixed rather than
re-scored: `renderer_name()` answers from the compiled-in features, and
`--features software` keeps the `femtovg` default on, so every swept PNG carried a
sidebar footer reading `FemtoVG · GL` over a picture `HeadlessPlatform` — Slint's
software rasterizer — actually drew. That is not cosmetic here: the sweep is the
project's evidence *about rendering*, and it had been captioning itself with a
renderer it never used (the same trap `scroll_ab.ps1` documents for a `--features
skia` build). `quire-shot` now overwrites the property with `Software · headless`
after `wire()`, and the app binary is untouched, because the app really does run
the backend its features select. The re-baseline is the proof of scope: 53 scenes
moved 118–125 px in the footer's single caption line, `settings` 244 px across that
line plus the dialog's Renderer row — the two places that read the property, and
nothing else.

The A4 open list this snapshot used to carry (1 HIGH + 2 MEDIUM + 3 LOW + 1 known
limitation) is now: **closed** — D1 (marked lines did not wrap, ADR-0041), D9 (the
empty state's "later milestone" copy, ADR-0033 — no such string is in `ui/`
anymore), the modal scrim, the `Ctrl+B` palette chord (sidebar is `Ctrl+\`), the
renderer label above, and both contrast LOWs. The label one is what taught the
last: the two contrast findings went from eyeballed to measured, one half of the
second was false, and the pair that really was weakest had never been painted.
**The A4 list is now empty.** D7 · MEDIUM — "per-page find reports 2 / 16 and
paints no individual match" — closed by ADR-0043: a hit is one word-run cell with
a measured fill and a border that does the marking, the bar repaints only the
rows that carry one, and the cell is drawn wherever body text is drawn — a
block's own line, a grid cell, a box in a columns layout, a quote, a callout.
The finding's own diagnosis was right that this is the one-`Text`-one-colour wall
ADR-0042 went around for code, and the way around it here is different: the layer
trick needs a monospace font, a background does not. What closed it was the
count, not the code: 16 hits on the swept page are 14 cells plus the 2 in the
block currently under the caret, which answers with a real text selection.

`.scratch/sweep31` (57 scenes) was that slice's baseline; `.scratch/sweep32`
(64 scenes) is the current one. Two scenes moved and three are
new: `find` and `dark-find` gained 4428 px of `#ffe9a8` / `#3d3413` and 756 px of
`#bd6408` / `#a87718` each — 9 boxes apiece, at identical coordinates in both
themes, which is the evidence that the geometry is the marker's and not the
palette's — and `find-grid` / `find-cols` / `find-callout` search a word that
lives in a table cell, in a columns box and in a callout, painting 1, 2 and 2
boxes there. `find-grid`'s term had to be changed to one that exists only inside
the grid: with the bar's own "the" it painted seven boxes on the page and none in
the cell it was meant to prove, which is a scene that passes either way. The
other 52 scenes are byte-identical.

The contrast pair, measured rather than judged (WCAG relative luminance over the
four light surfaces, `.scratch/contrast.py` for the before table and
`.scratch/contrast_after.py` for the after): light `text-muted` was 2.71:1 on
white and 2.51 on the sidebar — the palette's third tier, which the block handle,
the footer, the sidebar and every hint row in the palette and search panel paint —
and is now 4.43 / 4.10. The two weakest block slots were orange-on-its-own-tint at
2.81 and yellow at 2.48; they read 3.61 and 3.56, and on a plain background 4.21
and 3.95. `#d9730d`/`#cb912f` → `#bd6408`/`#a87718`, dark untouched. What the
measurement also did was retract: the sweep's named pairs, green-on-olive and
red-on-black, measure 3.86 and 4.72 and were never the problem. And the ceiling is
now explicit rather than assumed — muted cannot simply go to 4.5 on every surface,
because the next step down is `text-secondary` at 5.91 and a third tier that sits
1.0 above the second is one tier. The dark muted has read 3.9–4.4 all along, so
that band, not the AA line, is where both themes meet.

M12's first slice (ADR-0044, a page's own typography) is the one where the sweep
*is* the gate, because no memory number applies: 119 `.slint` call sites moved
from `Typography.*` to a derived `PageType.*`, and the claim is that nothing
outside the page's own text column moved with them. `sweep31` → `sweep32` reads
**1 of 57 moved, 7 new, 56 byte-identical**, and the mover is `menu.png` at
1 430 px confined to x 240..423 / y 542..779 — the Style submenu's own box. The
new arms are then measured against `default.png` with a self-vs-self control at
0 px: serif 63 638, mono 62 780, small text 55 285, full width 51 533, all three
68 779 against the serif shot, dark serif 63 140. The reading that separates a
layout switch from a font swap is where the text column starts: x 390 for the
first three, **x 284** (= sidebar 260 + `Theme.spacing-xl` 24) for the two
full-width arms. What is *not* on record is a style switch on the 10 000-block
bench page — untimed, and `docs/PERFORMANCE.md` says so rather than implying the
56 identical scenes cover it.

M12's second slice (ADR-0045, a page's own icon) is the same gate read in the
opposite direction: **64 of 64 scenes moved**, and they were meant to, because
every iconless tree row now paints its title's first character where it painted a
generic page glyph. A still baseline would have been the failure here. What makes
it evidence rather than noise is the shape of the movers — 72 937 px, the modal
scene 1 153 px inside x 13..52 / y 365..713, and the same pass restricted to
x 53 rightward at **0 px in 63 of the 64**, `menu.png` alone at 345 px for its own
one-row-taller popup. That second number is the one the slice could have failed:
giving a parent row an icon box *beside* its chevron, or applying the initial to
Favorites and Recent, would each have moved every scene past x 53, and both were
in the first build.

M12's last slice (ADR-0091, version history) is the one where the gate was not a
sweep. §三十八 refuses an unlimited retention policy in words — 保留策略必须给出磁盘与
RAM 数字 — so the deliverable is a number, and a number needs a command line that
produces it again: `cargo test --release --test backup version_cost -- --ignored
--nocapture`, an `#[ignore]`d probe kept in the suite precisely so the next person
who changes the fixture can re-weigh the cap. It paid for itself immediately. The
first build emptied the two FTS tables of every version file and its own tests
agreed — `SELECT COUNT(*) FROM search_blocks` reported 0 — while the file for a
60-line page weighed 1 560 576 bytes, because FTS5 keeps its term dictionary in a
`search_blocks_data` b-tree (368 rows, 1.4 MB) that a plain `DELETE` walks and
leaves standing. In other words the version of one page contained the words of all
201, and the assertion that would have caught it was not the one written. The fix
is `DELETE` plus FTS5's `rebuild` command, and the check moved onto `_data`'s row
count. Bytes after: 122 880 for that page's version, 1% of the library, with the
cap's worst case at 159% of it. The pixels then said what they always say —
`sweep38` → `sweep39` at **78 of 80 byte-identical**, the two movers being the page
⋯ menu at its two anchors for its fourteenth row, and the three new scenes each
confined to the panel's own box (x 410..868 / y 162..638), which is the difference
between a popup that grew and a layout that moved.
